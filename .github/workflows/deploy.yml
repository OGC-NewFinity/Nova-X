name: Deploy Nova-X

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H YOUR_SERVER_HOST >> ~/.ssh/known_hosts

      - name: Deploy via SSH
        run: |
          ssh YOUR_SSH_USER@YOUR_SERVER_HOST "
            cd /domains/collectivefinity.com/public_html/wp-content/plugins/Nova-X &&
            git pull origin main
          "
